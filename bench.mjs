import { parse } from './lib/index.mjs';

const buf = new Uint8Array([
  0x96, 0x01,
  0x96, 0x80, 0x01,
  0x01,
  0x96, 0x80, 0x85, 0x01,
  0x96, 0x01,
  0x96, 0x80, 0x01,
  0x01,
  0x96, 0x80, 0x85, 0x01,
]);

console.time('parse');
for (let i = 0; i < 1e6; i++) {
  parse(buf);
}
console.timeEnd('parse');
console.log(parse(buf));
